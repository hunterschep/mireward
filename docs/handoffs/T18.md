# T18: Toll evidence and the monastery crypt

Status: VERIFIED scene/domain integration and inspected puzzle geometry. Native campaign play and audible bell output remain later acceptance gates.
Repository checkpoint: root owns integration wiring, Git commits and pushes.

## Delivered behavior

`CryptContent` adds three waist-height chimes using the existing original art, visible Reed/Stone/Flame labels, lit markers with textual Set indicators, the adjacent canonical inscription, a closed iron vault gate, and the fixed charter coffer. Correct/reset/solved behavior calls the existing QuestService.ring_chime; there is no second sequence implementation or saved counter. The gate derives only from flags.puzzle_solved. Early solving and charter collection remain separate durable facts.

The four crypt hollow keepers use CampaignWorld's shared ordinary encounter population. Their IDs are distinct from the two monastery-yard keepers. Enemy death and zero-crown purses reuse the existing live lifecycle. The chime bank leaves the center aisle open; actual keeper navigation through the area is executed in the integration test.

The existing T17 physical toll notice/receipt and Elian station are exercised through actual InteractionRay, WorldObject, NPC and UI controls. MQ02 has no toll payment or required checkpoint defeat. MQ03 still requires Elian's distinct initial request and later attestation, including when the charter was collected early.

## Files and integration API

Owned files:

- `scripts/world/crypt_content.gd` and its UID.
- `tests/integration/test_monastery_arc.gd` and its UID.
- This handoff.

No map, CampaignWorld, shared service, UI or base interior geometry file was edited by this task. The T17 owner supplied `CampaignWorld.populate_encounters(world, candidate) -> ActionResult`, which requires the base CampaignPopulation and rejects repeated population.

```gdscript
var crypt := CryptContent.new()
crypt.build(world: Node3D, detached_snapshot: Dictionary) -> ActionResult
crypt.activate(world: Node3D) -> ActionResult
```

Root composes the crypt scene as follows:

1. Call the existing `campaign.build(world, candidate)`.
2. For `interior_crypt`, call `campaign.populate_encounters(world, candidate)` to create the four ordinary keepers with one coordinator.
3. Call `crypt.build(world, candidate)` before navigation/placement validation.
4. After committing/activating the world, retain the existing generic WorldObject and CampaignPopulation activation, then call `crypt.activate(world)`.

Construction uses only the detached solved flag and saved source records. It connects no live listeners and cannot advance a quest. Repeated build calls reject duplicate identities. Activation is idempotent; tree exit disables chime interaction and disconnects evidence/session listeners. Root's normal `world_activated` rollback hook reactivates the same puzzle without losing transient progress. Actual save/load resets partial progress through the already-owned QuestService.reset_runtime.

## Physical registry

| Entity | Position (X,Y,Z) | Purpose |
|---|---|---|
| `crypt_bell_puzzle` | `(0,0,-32)` | Objective target and puzzle controller |
| `crypt_reed_chime` | `(4.4,0,-30)` | Ring reed; faces west into the room |
| `crypt_stone_chime` | `(4.4,0,-32)` | Ring stone; faces west |
| `crypt_flame_chime` | `(4.4,0,-34)` | Ring flame; faces west |
| `crypt_vault_gate` | `(0,0,-35)` | Covers the full final opening; collider 4.08×3.1×0.4 m |
| `abbey_inscription` | `(-4.4,0,-32)` | Canonical readable clue |
| `charter_vault_coffer` | `(0,0,-40)` | Fixed `orra_charter` source |

Chime IDs register their InteractionComponent in world.entities. The puzzle root and gate are runtime entities whose appearance derives from existing state; they add no saved world fields. Gate collision is present while unsolved and removed only when solved. The raised grille visually clears the opening. Each chime has its own avoidance obstacle. The four keepers retain the exact manifest positions and IDs `crypt_hollow_keeper_01` through `_04`.

The clue is the existing canonical text: “Reed for the traveler. Stone for the shelter. Flame for the watch. Offer them in the order of mercy.” Elian can state the order through her existing dialogue. Wrong input reports the reset in visible text and clears markers without damage, money loss or a transaction receipt. Correct inputs display the current count; solved chimes explain that the vault is already open.

Sound requests use `bell_low`, `bell_chime` and `bell_solved` through AudioService.play_event. T24 must provide audible assets/playback for these IDs. The current test verifies muted operation and visible feedback, not actual sound output.

## Verification actually performed

Pinned Godot 4.5.2 on macOS:

```sh
"$HOME/.local/share/mireward-tools/godot-4.5.2/Godot.app/Contents/MacOS/Godot" --headless --path . --editor --import --quit
"$HOME/.local/share/mireward-tools/godot-4.5.2/Godot.app/Contents/MacOS/Godot" --headless --path . --script res://tests/run_all.gd -- --suite=integration/test_monastery_arc.gd
```

Import passed. Final focused suite: **PASS 262 assertions, zero failed suites**, below the default 120-second timeout. Output: `/tmp/mireward-t18-tests.log`.

The suite verifies detached construction with unchanged live state/listener counts; exact four-keeper registration and separation from the exterior pair; real keeper approach through the chime area; shared death/purse handling; physical/UI MQ01 and MQ02 handoffs with living checkpoint soldiers; exact MQ03 request/attestation/reward; visible text labels and clue with all sound volumes muted; incorrect and duplicate-symbol reset; marker progression; actual player capsule blocked by the closed gate and crossing the open gate; real partial and solved save/load; retained keeper defeat; charter collection separate from solving; early exploration before acceptance; once-only attestation; and failed final-arrival rollback preserving the same partial puzzle and one listener.

The tests position the real player at validated approach points to isolate scene interactions. They inject damage through CombatComponent to clear keepers for puzzle/save tests. Those fixtures do not establish combat skill, unassisted traversal or native hardware play. The keeper approach and gate-crossing checks do execute normal physics; the quest/evidence/reward/save changes use actual production APIs and UI controls.

Two background minimized GPU captures were generated and inspected at 1280×720 using Compatibility/OpenGL on Apple M3 Max: `tests/output/crypt/vault_closed.png` and `vault_open.png`. The ignored fixture is `tests/output/crypt/capture.gd`/`.tscn`; log `/tmp/mireward-t18-capture.log`. The open image derives from the real three-step QuestService solution. These images inspect this module's staged geometry, text and markers without simulating UI or encounters, and do not claim a native playthrough.

## Reproducible checkpoint saves

The normal runner removes isolated saves. To retain the test's generated slots for local inspection, run its worker explicitly with an ignored output directory:

```sh
mkdir -p tests/output/monastery_arc
MIREWARD_TEST_SAVE_DIR="$PWD/tests/output/monastery_arc" "$HOME/.local/share/mireward-tools/godot-4.5.2/Godot.app/Contents/MacOS/Godot" --headless --path . --script res://tests/run_all.gd -- --worker --suite=res://tests/integration/test_monastery_arc.gd
```

`manual_1` is pre-MQ03-completion with two transient chimes at save time; loading correctly restores zero. `manual_2` has the solved gate before charter pickup. `manual_3` is a validated completed-MQ03 exterior checkpoint after the early-explorer route. No fixture is copied into production save slots. The production GameRoot now applies this build/activate wiring. The final262-assertion root rerun used that production builder and checked its activation before fixture callbacks.

## Remaining gates

Root owns final production composition and shared verification records. T24 owns audible bell feedback. T23/T25/T28 own final art/readability, broad native input, unassisted campaign and exported-build acceptance. The test's spatial fixture placements are disclosed and must not be substituted for those gates.
