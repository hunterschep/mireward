# T19: Wren, the watchtower and Names in the Ledger

Status: VERIFIED production scene/UI integration and inspected ledger art. Native unassisted play remains a later acceptance gate.
Repository checkpoint: root owns review, commits and pushes.

## Delivered and verified

The existing T17 population and T12/T13/T15 services already implement the complete MQ04 arc. This task added `tests/integration/test_ledger_arc.gd`; no production adapter, shared service, UI, map or CampaignWorld change was required.

Both amnesty and restitution execute through the actual MireGameRoot: physical Wren acceptance, separate account meeting, watchtower ledger pickup, explicit material-choice review/cancel/confirmation, and Mara report. Key interactions use bound E events through the real InteractionRay; UI controls are focused and activated with Enter. Save/Load panels write and reconstruct actual files before and after the promise and after the report. Retained stale UI callbacks are deliberately invoked to verify rejection and once-only effects.

The canonical watchtower actors are two deserter raiders and one cutpurse. Camp residents, courtyard wardens, Wren and Ada retain neutral collision with no hostile hurtboxes or combat adapters. The badge locker is usable before SQ05 acceptance. The early-ledger branch collects it while MQ04 is AVAILABLE but unaccepted; acceptance and Wren's separate meeting reconcile that evidence without automatically choosing terms.

Reviewing or cancelling either promise leaves the entire snapshot unchanged. Confirmation stores `choices.wren_terms` and the canonical `choice/wren_terms` receipt without spending/awarding crowns or consuming the ledger. The confirmed quest becomes READY. Mara's real report pays exactly 30 crowns once, completes MQ04 and makes MQ05 AVAILABLE. Her stale callbacks cannot duplicate that reward. Ada physically offers MQ05 under both branches, while undercroft access still correctly waits for her later evidence handoff.

Wren's grain context and No Clean Hands options remain available, and SQ06 can be accepted after either promise. Wren has no authored shop; the test instead verifies the genuine Oswin shop still opens. Both branch outcomes have equal inventory, carried main evidence, report money, MQ05 availability and prior loot records.

## Reproducible checkpoints

The pre-MQ04 fixture uses root-approved helpers from `test_monastery_arc.gd` on the same production root. It completes MQ01/MQ02, obtains Elian's request, enters the actual crypt, solves its real chimes, picks up the charter and receives actual Elian attestation. No completed quest or promise flags are fabricated.

A disclosed damage fixture defeats the two old cart actors through CombatComponent. One purse is opened and retains four crowns; the other is collected through its real UI. This preserves a partially looted encounter using the canonical loot model, which has no fractional-crown pickup API. Four prior crypt defeats, the unclaimed purse and the depleted purse remain intact through every branch/reload.

Retained files are under ignored `tests/output/ledger_arc/checkpoints/saves/`:

| Slot | State | Crowns | Carried evidence |
|---|---|---:|---|
| `manual_3` | Post-MQ03; MQ04 AVAILABLE; no Wren terms | 84 | Toll receipt and charter |
| `manual_1` | Amnesty; MQ04 COMPLETED; MQ05 AVAILABLE | 114 | Receipt, charter, ledger and badge |
| `manual_2` | Restitution; MQ04 COMPLETED; MQ05 AVAILABLE | 114 | Receipt, charter, ledger and badge |

Both final saves have 100 health/stamina, exterior scene, village_shrine anchor, position (-110,0,149), and SQ06 ACTIVE with medicine recipient unset. SQ05 remains AVAILABLE. The 84-crown baseline is the legitimate 80-crown MQ01-MQ03 total plus one four-crown purse; neither terms branch adds hidden currency.

Regenerate without touching production save slots:

```sh
mkdir -p tests/output/ledger_arc/checkpoints
MIREWARD_TEST_SAVE_DIR="$PWD/tests/output/ledger_arc/checkpoints" godot --headless --path . --log-file "$PWD/tests/output/ledger_arc/verification.log" --script res://tests/run_all.gd -- --worker --suite=res://tests/integration/test_ledger_arc.gd
```

The ordinary isolated/cleanup command is:

```sh
godot --headless --path . --script res://tests/run_all.gd -- --suite=integration/test_ledger_arc.gd
```

## Verification actually performed

Pinned engine: Godot `4.5.2.stable.official.6ce3de25a` on macOS. The final retained worker completed **374 assertions, zero failures**, exit 0, with no script/engine errors. The run stays within the normal runner's 120-second watchdog. Log: `tests/output/ledger_arc/verification.log`.

A 14.275-meter local watchtower route was walked twice using ordinary movement input and production physics, without jumps or repositioning during either walk. It starts at (182,0,-218), skirts the ledger chest through (181.2,0,-219.5), (176,0,-220), (176,0,-222.8), (177,0,-222.8), then crosses the actual doorway/ramp to (177,0.168,-226.1). Final waypoint errors were below 0.077 m. `route_report.json` records both runs. Initial fixture waypoints crossed a side wall or approached the door flank, and the first steering implementation overshot tight corners; those fixture failures were corrected using the real doorway and ordinary W release for braking. No collision was removed or weakened.

The spatial fixtures position the player at validated interaction approaches. Damage injection clears crypt/watchtower actors for story, save and doorway checks; it is not a player-won fight. The long valley routes remain covered separately by T17's populated route walks. This task does not claim an unassisted camp-to-watchtower trip or native hardware input.

One background minimized GPU process rendered and exited in 3.52 seconds using Compatibility/OpenGL on Apple M3 Max. Both 960×540 close-ups were opened and inspected:

- `tests/output/ledger_arc/watchtower_ledger_front.png`
- `tests/output/ledger_arc/watchtower_ledger_side.png`

The grounded ledger chest is visibly separate from the raised doorway floor, with its lid, trim and front emblem intact; no burial or floating placement was identified. These use the actual game world viewport, so an OS-driven pause overlay cannot obscure the art. They establish ledger-detail appearance, not visual UI/interaction acceptance. Capture fixture/log/launch metadata are in the same ignored directory. The earlier T17 paused ledger-detail frame remains accurately recorded as limited evidence.

## Continue into MQ05

Load `manual_1` or `manual_2` with `await game.load_slot(slot_id)` in a process using the retained isolated save directory. Both restore at the village safe anchor. Visit Ada at (44,0,-227), choose `dialogue/accept_mq05`, then separately `dialogue/record_present_evidence`. The existing service checks retained charter and ledger before setting undercroft_open. T20 owns the actual Rusk actor/duel, seal source and subsequent report. No production save location was changed, no game was published, and this task made no Git mutations.
