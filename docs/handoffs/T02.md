# T02: Shared foundation
Status: VERIFIED for the foundation gate, with story-specific validation assigned to T12/T13/T14.

The five autoloads, typed definitions/results, frozen JSON registries, stable IDs, InputMap/collision contracts, one session, staged transactions, event bus, content validator, and isolated test runner are integrated. See contracts.md and T02-audit.md for exact semantics and owned boundary fixes.

Verified with the pinned engine: fresh import without parse errors; content validator; 48 consequential foundation assertions; 11 combat-math/cone assertions. The runner itself rejects assertion, parse, and runtime failures with nonzero exits. A visible foundation shell launched and keyboard Quit worked.

All twelve quests and eight dialogues are reserved definitions marked foundation_only; release validation correctly rejects them. Audio and save service entrypoints remain explicitly incomplete until their tasks. This closes shared foundation readiness, not the complete game requirements.

CombatMath is the single formula implementation for T06/T07/T20. Session JSON uses string keys and values; transient events use StringName IDs. Transactions alone own receipt history, with notifications after commit. Settings are independent SaveService.DEFAULT_SETTINGS fields. Tests use a unique MIREWARD_TEST_SAVE_DIR and may inspect t.test_save_directory.

Next: finish T03 art families, integrate T04/T05, then T06 before parallel ordinary enemies and inventory work.
