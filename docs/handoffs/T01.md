# T01: Project foundation
Status: VERIFIED for the foundation gate; release remains open.

## Delivered
Godot 4.5.2.stable is pinned. The project uses Compatibility, 60 Hz physics, named collision layers, runtime-installed default InputMap actions, and five autoloads. Root design/requirements/tasks remain canonical. See T01-environment.md for the independently verified engine and export-template installation.

## Verification actually performed
- Pinned engine `--headless --path . --import`: exit 0 with no parse errors.
- Graphical launch `--path . --rendering-method gl_compatibility`: initialized OpenGL 4.1 Metal on Apple M3 Max.
- CUA screenshot inspected: title/tagline/foundation status and Quit render correctly.
- CUA Return on focused Quit closed the game; game process exited 0.
- This is a minimal title shell, not gameplay or final visual acceptance.

## Successor
Main scene is scenes/main_menu.tscn. Keep .godot and builds ignored. Matching macOS templates require `textures/vram_compression/import_etc2_astc=true`, now set. Preserve typed contracts in docs/contracts.md. T02 hardening continues before the shared foundation checkpoint.
