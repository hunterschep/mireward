# Verification evidence

Starting tracked repository: specification-only commit 102f6f9. Useful unfinished foundation files were preserved and completed.

## Executed checks

| Check | Result | Evidence |
|---|---|---|
| Pinned editor and matching export templates | PASS | handoffs/T01-environment.md; official archive checksums and external smoke logs |
| Godot 4.5.2 foundation fresh import | PASS | `--headless --path . --import`, exit 0, no parse errors |
| Foundation registry validator | PASS | 23 items, five enemy definitions, 12 reserved quest IDs, eight reserved speaker IDs, nine landmarks, 26 spawns |
| Initial foundation tests | PASS | `--headless --path . --script res://tests/run_all.gd`: 11 assertions, zero failures |
| Title-shell visual and keyboard Quit | PASS | CUA screenshot inspected and Return closed the process, exit 0; handoffs/T01.md |

These checks establish only the foundation. Reserved quest/dialogue definitions are explicitly rejected by release validation. Gameplay, final art/audio, full campaign input, save recovery, soak, and game export remain unverified until their task gates execute. The separate environment probe is not the game's tested build.

## Foundation hardening and movement

- T02 audit integrated: 48 foundation assertions pass, including deliberate runner assertion/parse/runtime failures; see handoffs/T02-audit.md.
- Combat formulas/cone checks: 11 assertions pass.
- T04 real-scene headless movement: 12 assertions pass at 30/60/120 render FPS, diagonal normalization, stationary/moving sprint, air-jump refusal, wall collision, pitch and invert-Y.
- Initial mouse fixture failed because the headless display cannot establish captured mouse input. Refactored the same look calculation into apply_look for the domain check. This does not claim real mouse capture verification.
- CUA inspected the rendered movement arena with sword, shield, hands, and articulated actor art; repeated real W presses moved the viewpoint. A later mouse-look attempt lost the game window and did not establish that check. Mouse-look visual verification remains open.
- T04 strengthened sprint checks: PASS 13 assertions after checking exact sprint speed and exhausted walking speed. Rendered player capture inspected at tests/output/player/movement_arena.png; handoffs/T04.md records the remaining mouse-input gate.
- Fresh archive of foundation checkpoint 70d1530 imported and ran. Its initially absent empty tests/integration directory produced a directory warning; runner now skips absent optional suite directories. Rerun against that clean archive with the corrected runner: PASS 59 assertions, no errors.
- T03 integration review: root inspected all three 540p showcase captures and reran test_art: PASS 748 assertions. Generated architecture/props use material batching, actors retain articulated pivots, and all 80 saved model scenes load. Provenance and deterministic-generation evidence are in handoffs/T03.md and asset_manifest.md.
- T05 final focused fixture: 33 assertions passed. Root reviewed and reran the earlier 30-case version; final owner rerun adds stale action/held-E/modal teardown checks and reports full suite853 passing. Production native cursor, actual alt-tab and full-panel visuals remain open at T11/T15/T25. See handoffs/T05.md.
- T06 integrated into the actual player scene: root reran79 combat unit assertions and23 original spatial assertions; owner expanded spatial coverage to26 for rotation/protruding-wall cases. Root then ran the complete integrated suite: PASS958 assertions, zero failed suites. Transcript captured at /tmp/mireward-t06-integration.txt.
- Root launched the graphical combat arena with --capture and inspected tests/output/combat/arena.png: actor and first-person gear render in Compatibility. This is a static rendered fixture, not combat-feel or full input verification. Font provenance/bytes were independently matched to pinned Open Sans1.10 and notices included in export filters.
- T08 root staged-diff review and integration: validated container IDs and bounded saved loot, wired shared services, and tested inventory/equipment against actual player combat. Review caught merged-candle and shared-key source gaps; fixed validation and added two regression fixtures. Final focused suite: PASS130 assertions. T08 physical loot/shop/menu wiring remains later work.
- Development export built from clean tracked checkpoint257d111, with matching4.5.2 templates and valid local ad-hoc signature. Independent executable headless combat-arena smoke exited0. CUA then resumed its focus-paused arena and sent three separate native left-mouse attacks; front cutpurse fell on the third. This establishes native attack execution in the development fixture, not a campaign/release build. Artifact: builds/development/MIREWARD.app. Subsequent mouse-look attribution was unclear while the window was being used, so full look/feel checks remain open.
- T07 implementation checkpoint: owner reports63 passing real-physics enemy assertions; root staged review and import passed. This is an in-progress checkpoint: RETURN hit-history preservation and unchanged live-state refresh are under correction with new regressions. The checkpoint does not close those gates. Native AI/crowd inspection remains unverified.
- T07 final root rerun: PASS67 assertions after reviewing RETURN history, unchanged-state refresh and reservation-expiry changes. Raw spawn-facing validation added with a failing-input fixture; foundation suite now49 assertions. Native AI controls and production world/loot wiring remain separate gates.
- T09 service checkpoint: root reviewed the economy/recovery source and autoload wiring, then reran test_economy_recovery: PASS105 assertions. Includes failure/retry receipts and explicit asynchronous arrival outcomes. Same-tick healing/damage and real-player timed-use integration are still under test, so T09 remains IN PROGRESS.
- T09 final root integration:107 unit and44 real-physics assertions passed. Pending recovery now holds travel mode and blocks movement/look/interactions across panel close and failed arrival, then releases on explicit completion. Same-tick damage, paused healing and progress-preserving rest/death remain covered. Actual campaign router/UI/disk autosaves are later gates.
- T10 root integration: re-ran test_exterior with634 passing assertions; compared all pre-existing map keys, values and array ordering against b8449aa with no changes. Reviewed generator/terrain/collision/decor source and six GPU captures. Traversal evidence covers2076.1m acrossfive scripted normal-speed routes. Baseline dressing is sparse and distant landmark contrast needs the final art pass; these images do not close R41/R50. Root corrected the capture camera from60 to the actual75-degree default and850m far plane for subsequent reviews.

- T12 root integration: reviewed all quest definitions and transaction/predicate contracts, wired acquisition/defeat reconciliation, reran619 quest assertions and233 exterior-manifest assertions with zero failures. Content validator passed canonical registries. This covers domain campaigns/side choices and malformed input; physical story sources, dialogue/UI and persistent save validation remain successor gates.

- T13 integration: eight authored speakers,107 nodes and69 topics; focused suite598 assertions and release content validation passed. Root wired dialogue creation and close on new/valid restore. Staged NPCs now defer live EventBus subscriptions and interaction until activation. Speaker-qualified node IDs reject callbacks from an older speaker. Native dialogue layout and campaign placement remain T15/T17 gates.

- T11 root integration: default travel612 assertions passed; review then exposed two real rollback defects. Full menu-stack restoration and reactivation of NPC listeners after a late failed arrival now pass19 presentation and45 mode assertions in root reruns. Owner reran all612 default travel assertions on final code. Three furnished interiors and physics/navigation-backed staged world swaps are implemented; unassisted campaign traversal remains open. See handoffs/T11.md.

- Persistent runtime composition: root focused test_game_root passes27 assertions, including actual window resize at4:3/21:9/portrait/extreme-wide, Retro/Native extent, player collision and identity across travel, native modal pause, and complete player/recovery teardown. Review found/fixed the default16:9 canvas aspect policy. Root inspected GPU exterior Retro and inn Native images in tests/output/runtime. Generated mouse-event testing while capture is active reports the same yaw delta in both rendering modes; this is not a native hardware-input campaign check. Main-menu and save/UI integration remain open.

- Fresh-checkout verification: exported tracked commitac1625e into /tmp/mireward-checkpoint-ac1625e.ZzjNBo, imported it with the pinned editor, ran release content validation, and ran every committed suite. Result: PASS4015 assertions, zero failed suites. This archive includes T11–T13 and the pure initial snapshot builder, before the separate runtime graph checkpointb6984d4. It establishes reproducibility of that source checkpoint, not a finished game release.

- T14 executable checkpoint: root264 save-codec assertions pass, covering exact envelopes, real campaign round trips, backup/rename failure injection, unreadable-primary preservation, invalid IDs/story locations, settings and remapping. Root composed-runtime suite passes46 assertions including three actual title/load cycles, stable player/listener counts, reset/settings retention and unchanged manual slot bytes. Save failure/ending queue integration is still underway: review found that a failed ending save latch must be released by a later meaningful autosave request after Continue without saving. This checkpoint does not close that pending fix or T14 final gate.

- T14 final focused integration: root reran374 codec/settings and73 real-router runtime assertions, both pass. A new meaningful autosave request now permits one retry after a failed save, so real inn rest persists an ending after explicit Continue without saving. The failure latch still prevents repeated writes each frame; explicit unsaved continuation itself does not retry. Malformed equipment/player shapes and actual unreadable-primary preservation are covered. T14 native UI/export checks remain with successor tasks; handoffs/T14.md records the schema and final backend evidence.

- T15 training target:12 focused real-physics assertions pass. Actual light/heavy/light sweeps each hit the reusable straw target once; it awards no money, inventory, quest progress or persistent receipt. Staging and deactivation disable its hurtbox/interaction. It provides instructions for attack/block/parry and does not attack the player. Opening placement and native visual review remain pending.

- Physical loot/readable adapter integration: 161 assertions in the root rerun pass after source review. Covers all 13 fixed sources, corpse currency, permanent open/depleted rendering, quest gates, actual reach, occlusion, player collision and staged/live listener teardown. These reusable objects are ready for placement; physical campaign routes remain an integration gate. See handoffs/T08-world-objects.md.

- T15 main UI checkpoint: root reran108 actual UI assertions with zero errors, plus46 composed runtime and47 mode assertions. The main scene now has functional menus/HUD and the obsolete title shell is removed. Initial background GPU captures of title, HUD, inventory, settings, dialogue, map and load panels were inspected. Review fixed map overflow, dialogue row limits and HUD contrast; geometric UI tests cover the corrections. A later minimized capture stalled after the title without diagnostics and remains under investigation, so current native New Game/campaign acceptance is not claimed. A completed-writ transaction replay also exposed and fixed a reachable save-wait softlock; it now offers read-only epilogue replay.

- T15 capture follow-up: the original background run subsequently completed all eight frames with no errors. A separate instrumented native run also reached the exterior after successful New Game. Root inspected the corrected 150% map and dialogue captures: Back stays inside the window and exactly three choices appear before scrolling. HUD text is outlined over the pale world, and New Game faces the authored village road. No New Game deadlock reproduced; native hardware-input opening/campaign checks remain open.

- Clean source checkpoint91e55d5 was archived at /tmp/mireward-checkpoint-91e55d5.MSlcAF, imported, and validated in release content mode. Its full suite passed4,852 assertions with zero failed suites before the opening population was added.
- T16 opening population integration: root149 opening-world assertions and46 composed-runtime assertions pass. Actual factory/UI integration exposed a duplicate enemy ID in the older UI fixture and the straw target being labeled hostile; the fixture now uses the registered actor, and the HUD excludes the straw target. Root reran113 UI assertions successfully. Mara, Oswin, Tamsin, sign, training target, cart/coffer, two cutpurses and persistent purses now populate the production graph. Native S01 play, opening pacing and host build smoke remain next.

- Opening visual corrections: root inspected the blue medicine coffer and supported inn lantern GPU frames, then reran749 art and628 travel assertions successfully. Geometry/anchors and source identities are retained. Asset regeneration changed only the medicine chest, palette atlas and new paint material; the owner verified repeatable bytes. See handoffs/T16-visual-fixes.md.
- Native export/input check on23 September: the independently signed5d94f94 macOS app launched with a fresh isolated save directory, and native Return started New Game. Native title/gameplay/pause screens were inspected. Repeated later input requests were rejected by the control tool as external window changes, including immediately after a fresh state request. Sustained native traversal is UNVERIFIED; no full opening or campaign playthrough is claimed from this attempt. Normal-input software traversal is being added separately, with its input mechanism disclosed.

- Normal-input opening walkthrough: the opt-in route passed50 assertions, walking438.71 m in116.07 seconds at60 Hz and time_scale1. It used movement/interact/Enter events, the real eye ray, actual UI, and no position/velocity/domain writes. MQ01 paid18 crowns and one bandage, MQ02 became available, and purchase/rest/manual save/load left21 crowns and100 HP. Headless aiming used the production apply_look method; native hardware input remains unverified. See handoffs/T16-walkthrough.md for the exact mechanism and report.
- The walkthrough exposed a real held-key bug: hint persistence reapplied unchanged InputMap events and dropped movement while W remained held. Settings now replace only changed bindings. Root383 codec/settings assertions and the strengthened8-assertion ten-meter movement smoke pass, including held input across unrelated settings writes and another action being remapped.

- Guard input review reproduced two improper parries: holding block through guard break or an attack restarted eligibility without another press. The component now distinguishes a requested guard press from forced lowering. Both regressions fail before the fix and pass afterward; root82 combat,26 spatial and47 modal assertions pass. Public combat APIs and ordinary block recovery timing are unchanged.

- T17 exterior integration: root reviewed the population and route changes and reran 296 content assertions successfully. All 18 exterior hostiles, 11 fixed sources, six exterior speakers, Tamsin, readables and neutral residents are present. Semantic JSON comparison confirmed only Elian X195 to197 and badge-locker Y0 to0.168 changed; deterministic navigation was rebaked. Five populated routes passed with static AI and checked physical sidesteps around frozen enemies. Root inspected Wren and badge-locker GPU views. Native combat/campaign acceptance remains open; see handoffs/T17.md.
- Test watchdogs now measure wall time independently of Engine.time_scale. This preserves the existing 120-second limit and the explicit 360-second opening-walk exception while allowing the established accelerated route fixture to run without an accidental fifteen-second deadline.

- T18 production integration: GameRoot now composes the crypt keepers, chimes, clue, charter coffer and durable vault gate. Root reran 262 assertions using the real production builder and activation, covering physical/UI MQ01-MQ03, early charter, gate collision, partial/solved saves and rollback. Closed/open GPU views were inspected. Puzzle sound requests are present; audible output remains T24 work. Native campaign acceptance remains open.

- T19 watchtower integration: root reran 374 assertions with zero failed suites. Both Wren promises use the real physical interactions, confirmation UI and save files; old defeats/partial loot, early ledger and badge, Mara's exact once-only reward and Ada's subsequent offer persist. The owner inspected two ledger GPU close-ups and walked the 14.275 m doorway route twice with normal input. Position/damage fixtures isolate the wider story checks; this is not a full unassisted campaign run. See handoffs/T19.md.

- Load policy correction for T20: encounter danger alone no longer refuses a load. Committed player actions, healing, recovery, transactions and travel still must finish; dangerous saves remain blocked. Root74 save-runtime assertions pass, including a real paused load during forced encounter danger. Actual living-boss reset remains part of the T20 encounter test.

- T21 reaction integration: root reran73 assertions against production GameRoot construction and activation, with genuine SQ04/SQ05 completion, detached preparation, paused updates, save reload, rollback and listener cleanup. Root inspected the three shelter candles and Ada's returned badge GPU views. Their exact saved flags control visibility; they add no local lights. Full side-quest matrix remains underway; see handoffs/T21-reactions.md.

- T22 adapter support: the road gate retains its named banner independently of batched masonry, and current policy notices can open the native readable panel without changing historical evidence. Regeneration changed only the road gate asset. Root750 art and114 UI assertions pass; ending-specific physical projections remain under implementation.
